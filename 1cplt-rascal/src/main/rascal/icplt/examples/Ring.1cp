global @node(neigh: @node, x, y: number)
{
  main:
    x -> neigh.y |> forward

  forward:
    if
      x < y
    then
      y -> neigh.y |> forward
}

process @node[1](@node[2], 11, 0)
process @node[2](@node[3], 17, 0)
process @node[3](@node[1], 13, 0)
